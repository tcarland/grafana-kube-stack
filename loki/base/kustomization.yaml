---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
- namespace.yaml

secretGenerator:
- name: s3-secret
  envs:
  - secrets.env

helmCharts:
- name: loki-distributed
  includeCRDs: true
  releaseName: loki
  namespace: monitoring
  version: 5.6.0
  repo: https://grafana.github.io/helm-charts
  valuesFile: loki-values.yaml
  additionalValuesFiles:
    - loki-structuredConfig.yaml
